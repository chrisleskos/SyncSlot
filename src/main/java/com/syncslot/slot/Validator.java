package com.syncslot.slot;

public class Validator {
    /*  Rule 1:
     *   Parties should not have the same name
     */

    /*  Rule 2:
     *   If not recurrent, set 'to date' to null
     */

    /*  Rule 3:
     *   Duration list should be of length 1 or match the recurrence Interval list Length
     *      - If it's 1, it applies to all recurrences
     *      - If it's equal to recurrence interval list length, each value is respectively assigned
     *      - If neither, error.
     */
}
